# Expense-Tracker-API
Expense Tracker API is a Spring Boot RESTful application that allows a single user to manage expense categories and expenses. It supports full CRUD operations, input validation, pagination, sorting, summary reporting, and API documentation via Swagger. Designed to demonstrate clean architecture and backend best practices.
